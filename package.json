{
  "name": "surveysetu-root",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm run build",
    "install": "cd backend && npm install",
    "seed": "cd backend && npx prisma db seed"
  }
}
